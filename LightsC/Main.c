#include <stdio.h>
#include "../LightsC/TurnOffLeftToRight/TurnOffLeftToRight.h"
#include "../LightsC/TurnOnLeftToRight/TurnOnLeftToRight.h"

int main()
{
	int pairOf = 3;//const do not change
	int incrementBy = 2;
	int maxPixelValue = 128;
	int minPixelValue = 10;
	int arrayLength = 30;
	//TurnOnLeftToRight(pairOf, incrementBy, minPixelValue, maxPixelValue, arrayLength);
	//TurnOffLeftToRight(pairOf, incrementBy, minPixelValue, maxPixelValue, arrayLength);

	return 0;
}